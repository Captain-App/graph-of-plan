{
  "name": "graph-of-plan",
  "version": "0.1.0",
  "description": "Typed, agent-centric business-plan compiler",
  "type": "module",
  "scripts": {
    "check": "tsx kernel/validate-cli.ts",
    "compile": "tsx kernel/compile/compile.ts",
    "build": "npm run check && npm run compile"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
