MCP (Model Context Protocol) server running on Cloudflare Workers with sandbox execution.

Provides secure, capability-scoped code execution for AI agents via the MCP protocol. Built on Effect-TS and @cloudflare/sandbox.
