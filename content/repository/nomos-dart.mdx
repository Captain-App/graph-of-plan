Nomos implementation in Dart for Flutter applications.

Contains nomos_core, nomos_codegen, persistence adapters (Firestore, ArangoDB, memory), and storage backends. Powers the domain model layer in Flutter apps.
